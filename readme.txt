Local report server debug/run instructions:

1. Make sure to add the report server project root directory to the classpath as follows:
   From the debug menu, choose debug.  Click on the classpath tab.  Click on user entries.  
   Click on advanced.  Choose add folders and click ok.  Add the report server root directory.